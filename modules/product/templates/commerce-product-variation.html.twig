{#
/**
 * @file
 *
 * Default theme implementation for product variations.
 *
 * Available variables:
 * - content: Items for the content of the product variation.
 *   Use 'content' to print them all, or print a subset such as
 *   'content.sku'. Use the following code to exclude the
 *   printing of a given child element:
 *   @code
 *   {{ content|without('sku') }}
 *   @endcode
 * - attributes: HTML attributes for the wrapper.
 * - product_variation: The product variation object.
 * - product: The product object.
 * - product_url: The product URL.
 *
 * @ingroup themeable
 */
#}
<div{{ attributes}}>
  {{- content.product -}}
  {% if content %}
    {{- content|without('product_variation_attributes') -}}
  {% endif %}
</div>
