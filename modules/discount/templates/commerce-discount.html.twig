{#
/**
 * @file
 *
 * Default template for discounts.
 *
 * Available variables:
 * - attributes: HTML attributes for the wrapper.
 * - discount: The rendered discount fields.
 *   Use 'discount' to print them all, or print a subset such as
 *   'discount.name'. Use the following code to exclude the
 *   printing of a given field:
 *   @code
 *   {{ discount|without('name') }}
 *   @endcode
 * - discount_entity: The discount entity.
 * - discount_url: The discount URL.
 *
 * @ingroup themeable
 */
#}
<article{{ attributes }}>
  {{ discount }}
</article>
