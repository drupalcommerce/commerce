{#
/**
 * @file
 *
 * Default theme implementation for stores.
 *
 * Available variables:
 * - store: Items for the content of the store.
 *   Use 'store' to print them all, or print a subset such as
 *   'store.name'. Use the following code to exclude the
 *   printing of a given child element:
 *   @code
 *   {{ store|without('name') }}
 *   @endcode
 * - attributes: HTML attributes for the wrapper.
 * - store_entity: The store object.
 * - url: The store URL.
 *
 * @ingroup themeable
 */
#}
<article{{ attributes }}>
  {{ store }}
</article>
